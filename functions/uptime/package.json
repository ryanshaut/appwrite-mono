{
    "name": "sample-uptime-client",
    "version": "1.0.0",
    "description": "Returns the server's current timestamp",
    "main": "src/main.js",
    "type": "module",
    "scripts": {
        "format": "prettier --write ."
    },
    "dependencies": {
        "mysql2": "^3.9.3",
        "node-appwrite": "^9.0.0"
    },
    "devDependencies": {
        "prettier": "^3.0.0"
    }
}
